# Python dependencies
# Note: This project uses Python standard library only
# No external packages required, but keeping this file for Railway compatibility

# Empty requirements file - Railway needs this to detect Python project
